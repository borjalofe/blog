{
  "name": "blog",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "npm run validate:ofm && npm run validate:contract",
    "validate:ofm": "markdownlint-obsidian content/**/*.md",
    "validate:contract": "node scripts/validate-content.mjs"
  },
  "devDependencies": {
    "markdownlint-obsidian-cli": "^1.3.2"
  },
  "engines": {
    "node": ">=20"
  }
}
